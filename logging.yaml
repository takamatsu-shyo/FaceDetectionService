LOGGING:
    version: 1
    handlers:
        console:
            class : logging.StreamHandler

        file:
            class : logging.handlers.RotatingFileHandler
            filename: ./log/nameko.log
            maxBytes: 1024
            backupCount: 3 
        
    root:
        level: DEBUG
        handlers: [console, file]
